import { notFound } from "next/navigation";import { ChevronLeft, FileText, FlaskConical, HeartPulse, Pill, Shield } from "lucide-react";import Link from "next/link";import { api } from "@/lib/api";import { age } from "@/lib/utils";import { Badge, Card } from "@/components/ui";import { PatientReview } from "@/components/patient-review";
export default async function PatientPage({params}:{params:Promise<{id:string}>}){const {id}=await params;let patient;try{patient=await api.patient(id)}catch{return notFound()}const findings=await api.findings(id);const sections=[{title:"Diagnoses",icon:HeartPulse,items:patient.diagnoses||[],render:(x:Record<string,string>)=>`${x.name} · ${x.code}`},{title:"Medications",icon:Pill,items:patient.medications||[],render:(x:Record<string,string>)=>`${x.name} ${x.dose} · ${x.frequency}`},{title:"Labs",icon:FlaskConical,items:patient.labs||[],render:(x:Record<string,string>)=>`${x.name}: ${x.value} ${x.unit} · ${x.date}`}];return <div className="mx-auto max-w-7xl space-y-7"><Link href="/patients" className="flex items-center gap-1 text-sm font-semibold text-slate-500"><ChevronLeft size={16}/>Patients</Link><div className="flex flex-col justify-between gap-3 md:flex-row md:items-end"><div><div className="flex items-center gap-3"><h1 className="text-3xl font-bold">{patient.name}</h1><Badge tone={patient.risk_level==="high"?"rose":"amber"}>{patient.risk_level} urgency</Badge></div><p className="mt-2 text-slate-600">{age(patient.date_of_birth)} years · {patient.primary_condition} · {patient.payer}</p></div><Badge tone="blue">{patient.workflow_status}</Badge></div><div className="grid gap-6 xl:grid-cols-[1fr_1.15fr]"><div className="space-y-5"><div className="grid gap-4 md:grid-cols-3">{sections.map(s=><Card className="p-4" key={s.title}><p className="mb-3 flex items-center gap-2 text-sm font-semibold"><s.icon size={17} className="text-teal-700"/>{s.title}</p>{s.items.length?s.items.map((x,i)=><p key={i} className="mb-2 text-sm leading-5 text-slate-600">{s.render(x)}</p>):<p className="text-sm text-slate-400">No records</p>}</Card>)}</div><Card className="p-5"><h2 className="flex items-center gap-2 font-semibold"><Shield size={17} className="text-teal-700"/>Insurance information</h2><dl className="mt-4 grid grid-cols-2 gap-4 text-sm">{Object.entries(patient.insurance||{}).map(([k,v])=><div key={k}><dt className="capitalize text-slate-500">{k.replace("_"," ")}</dt><dd className="mt-1 font-medium">{v}</dd></div>)}</dl></Card><Card className="p-5"><h2 className="flex items-center gap-2 font-semibold"><FileText size={17} className="text-teal-700"/>Clinical documentation</h2>{patient.notes?.map(n=><div className="mt-4 rounded-lg border border-slate-200 bg-slate-50 p-4" key={n.id}><p className="font-semibold">{n.title}</p><p className="text-xs text-slate-500">{n.date} · {n.author}</p><p className="mt-3 text-sm leading-6 text-slate-700">{n.text}</p><Badge>Source record</Badge></div>)}</Card></div><PatientReview patientId={id} initial={findings}/></div></div>}

